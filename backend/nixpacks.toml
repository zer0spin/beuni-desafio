# Railway Nixpacks Configuration for Backend
[phases.setup]
nixPkgs = ["nodejs-18_x", "npm-9_x"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npx nest build"]

[start]
cmd = "npm run start:prod"